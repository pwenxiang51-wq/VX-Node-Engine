# 🛡️ VeloX Node Engine 极客私有化登录手册

> **状态**：REPOSITORY PRIVATE (已封库)
> **目的**：解决私库权限导致的 404 报错，并激活暗号拦截系统。

### 1. 物理植入真灵魂
如果执行旧指令报错 `line 1: 404`，说明本地脚本已废。执行以下操作覆盖：
```bash
sudo nano /usr/local/bin/vx
```
*（清空内容，将 GitHub 私库中的最新的 `vx.sh` 源码全部粘进去，然后按 Ctrl+O（保存），回车确认，再按 Ctrl+X（退出））*

### 2. 赋予最高执行权
```bash
sudo chmod +x /usr/local/bin/vx
```

### 3. 装备大佬专属“金钥匙” (别名映射)
**关键步骤**：在系统底层建立关联，以后只需敲 `vx` 即可自动带密码开门。
```bash
echo "alias vx='/usr/local/bin/vx Velox'" >> ~/.bashrc && source ~/.bashrc
```

### 4. 验证测试
- 输入 `vx`：瞬间进入紫色全息大屏。
- 输入 `vx log`：完美绕过验证，直接看日志（多亏了 shift 黑科技）。
- 别人输入 `vx`（无别名）：直接吃 403 闭门羹。

---
*Generated by Gemini for 大佬 | 2026-04-16*
